export enum GAME_OBJ_NAMES {
    None = 'None',
    Game = 'Game',
}

export enum CHARACTERISTICK {
    Life = 'Жизни',
    Force = 'Сила',
}

export enum TYPE_FORCE {
    Light = 'Светлая',
    Dark = 'Тёмная',
    Neutral = 'Нейтральная',
}

export enum MISSION {
    Zero = 'Нулевая'
}

export enum ACTION_TYPE {
    Attack = 'Attack',
    Evasion = 'Evasion',
}