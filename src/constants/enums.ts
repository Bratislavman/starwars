export enum GAME_OBJ_NAMES {
    None = 'None',
    Game = 'Game',
}

export enum CHARACTERISTICK {
    Life = 'Жизни',
    Force = 'Сила',
}

export enum MISSION {
    Start = 'Нулевая'
}

export enum ACTION_TYPE {
    Attack = 'Attack',
    Evasion = 'Evasion',
}